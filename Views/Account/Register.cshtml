@model RealEstateAdmin.Models.RegisterViewModel

@{
    ViewData["Title"] = "Inscription";
}

<div class="container">
    <div class="row justify-content-center align-items-center min-vh-50 py-5">
        <div class="col-md-7 col-lg-6">
            <div class="card shadow-lg border-0">
                <div class="card-header text-center py-4" style="background: var(--gradient-primary); color: white; border-radius: 16px 16px 0 0;">
                    <i class="bi bi-person-plus" style="font-size: 3rem; margin-bottom: 1rem;"></i>
                    <h2 class="mb-0 fw-bold">Inscription</h2>
                    <p class="mb-0 mt-2 opacity-75">Créez votre compte gratuitement</p>
                </div>
                <div class="card-body p-4">
                    @if (ViewData.ModelState.ErrorCount > 0)
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <i class="bi bi-exclamation-triangle"></i> 
                            <div asp-validation-summary="All" class="mb-0"></div>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }

                    <form asp-action="Register" method="post" class="mt-3">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        
                        <div class="mb-4">
                            <label asp-for="Nom" class="form-label fw-semibold">
                                <i class="bi bi-person me-2"></i>@Html.DisplayNameFor(m => m.Nom)
                            </label>
                            <input asp-for="Nom" class="form-control form-control-lg" placeholder="Votre nom complet" />
                            <span asp-validation-for="Nom" class="text-danger small"></span>
                        </div>
                        
                        <div class="mb-4">
                            <label asp-for="Email" class="form-label fw-semibold">
                                <i class="bi bi-envelope me-2"></i>@Html.DisplayNameFor(m => m.Email)
                            </label>
                            <input asp-for="Email" class="form-control form-control-lg" type="email" placeholder="votre@email.com" />
                            <span asp-validation-for="Email" class="text-danger small"></span>
                        </div>
                        
                        <div class="mb-4">
                            <label asp-for="Password" class="form-label fw-semibold">
                                <i class="bi bi-lock me-2"></i>@Html.DisplayNameFor(m => m.Password)
                            </label>
                            <input asp-for="Password" class="form-control form-control-lg" type="password" placeholder="Minimum 6 caractères" />
                            <span asp-validation-for="Password" class="text-danger small"></span>
                        </div>
                        
                        <div class="mb-4">
                            <label asp-for="ConfirmPassword" class="form-label fw-semibold">
                                <i class="bi bi-lock-fill me-2"></i>@Html.DisplayNameFor(m => m.ConfirmPassword)
                            </label>
                            <input asp-for="ConfirmPassword" class="form-control form-control-lg" type="password" placeholder="Confirmez votre mot de passe" />
                            <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                        </div>
                        
                        <div class="d-grid mb-3">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="bi bi-person-plus me-2"></i>S'inscrire
                            </button>
                        </div>
                    </form>
                    
                    <div class="text-center mt-4 pt-3 border-top">
                        <p class="mb-0 text-muted">
                            Déjà un compte ? 
                            <a asp-action="Login" class="text-decoration-none fw-semibold">
                                <i class="bi bi-box-arrow-in-right me-1"></i>Se connecter
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
